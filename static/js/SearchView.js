define(['backbone'], function (Backbone) {

    var SearchView = Backbone.View.extend({
        initialize: function () {
            alert("Alerts suck.");
        }
    });

    return SearchView;
});

